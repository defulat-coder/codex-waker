# Ego Lite 本地验收记录（2026-08-28）

## 环境

- 工作区：`/Users/xbjt/Documents/myself/codex-waker`
- Web：`http://127.0.0.1:5210`
- API：`http://127.0.0.1:4410`
- Ego Lite task space：`4`（同一 Goal 全程复用，验收完成后 `completeTaskSpace(..., { keep: false })` 返回 `done: true`）
- 验收模型：`kimi-for-coding`，通过服务端 `@openai/codex-sdk` Thread API；浏览器未接触 provider key。

## 浏览器流程证据

| 范围             | 操作                                                              | 观察到的结果                                                                                      |
| ---------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| 首屏/导航        | 打开 Web，遍历 Waker、Chat、IM、WakerFlow、任务、项目、知识、设置 | 八个主入口均可达；桌面为 64px icon rail，窄屏为底部导航                                           |
| Waker            | 打开 Waker 管理，选择“头脑风暴”，点击创建对话                     | 进入对应 Waker 的 Chat composer；列表含 6 个本地 Waker 与配置入口                                 |
| Codex Chat       | 选择 `Kimi K2.7 Code`，发送“请根据本地知识库…”                    | 收到 thinking delta、text delta 与 done；最终回答引用 `docs/local-guide.md#L1-L5`                 |
| 刷新恢复         | 刷新页面，展开会话列表并重新打开最新会话                          | 标题为原始用户问题；用户/助手消息与引用完整恢复，未显示 Codex synthetic AGENTS bootstrap          |
| 知识文档         | 在“Waker 本地指南”点击新建文档，创建“Ego 验收文档”                | 文档数由 1 变 2；版本 1、source type `text` 可见                                                  |
| 关键词检索       | 查询“向量检索 FTS5”，选择 keyword                                 | 2 条结果；首条为 Ego 验收文档，引用 `#L1-L1`，score 1.000                                         |
| 向量检索         | 同查询，选择 vector                                               | 2 条结果；分别显示 vector score 0.289/0.108 与引用                                                |
| 混合检索         | 同查询，选择 hybrid                                               | 2 条去重结果；score 0.609/0.509，引用保持不变                                                     |
| 项目             | 创建“Ego 公开项目”                                                | 页面立即显示 `READY`；SQLite 中项目数为 2（含 seed）                                              |
| WakerFlow        | 创建“Ego 验收流程”                                                | 页面显示 `DRAFT`，seed 流程显示 `READY`                                                           |
| Automation/Board | 运行“每日项目摘要”                                                | API 返回 202；任务列表无需离开页面即从 1 更新为 2，状态 `PENDING`                                 |
| IM 降级          | 打开 IM                                                           | 明确显示“本地演示”“第三方连接器未配置”，本地通知为 `STOPPED`，不伪造外部连接                      |
| Settings         | 打开设置                                                          | 展示模型、thinking、资源与界面偏好；provider key 不返回浏览器                                     |
| Error/Retry      | 用 CDP 临时阻断 `local-resources`，打开项目页，再解除并点重试     | 出现 `加载失败 / Failed to fetch / 重试`；解除后恢复项目表格。该 loadingFailed 是有意注入并已清除 |
| 响应式           | CDP 设置 390×844                                                  | `scrollWidth=390`、`overflow=false`；6 张 Waker 卡可操作，导航固定底部                            |
| 深色模式         | 模拟 `prefers-color-scheme: dark`，1496×846                       | `dark=true`、rail width 64、无横向溢出；深色表面/文字/状态可读                                    |
| Console/Network  | 清除预期故障事件后汇总 Runtime/Log/Network                        | 未解释异常、console error、loadingFailed 或 HTTP ≥400：`count=0`                                  |

## 持久化回读

浏览器验收后只读查询 SQLite：

```text
knowledge.sqlite: notebooks=1 documents=2 versions=2 chunks=2 embeddings=2 bindings=1 audits=4
workspace.sqlite: projects=2 automations=1 workflows=2 channels=1 tasks=2
```

两篇当前文档各对应一个当前 chunk 和一个 embedding，无孤儿索引。最新真实 Chat session 的 API 回读只有一条原始 user message 和一条 assistant message；assistant 末尾引用 `docs/local-guide.md#L1-L5`。

## 扩展功能第二轮

第一轮 task space 已按规范关闭，因此确认 `listTaskSpaces()` 为空后创建 task space `7`；本轮完成后同样关闭并返回 `done: true`。

| 范围               | 实测结果                                                                                                                                                                                                                        |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Memory             | 读取 seed memory；创建编辑前/后快照；v1→v2 编辑；修复 UI 将 version ID 误传 diff API 的缺陷后得到 unified diff；rollback dry-run 与 apply 均成功，apply 自动生成 `pre_rollback`，文档变为 v3；Markdown import 创建第二篇 memory |
| Workflow           | active workflow 运行完成 `queued → running → waiting_input → running → succeeded`；另一次运行追加 `e2e.step`，Trace 显示 queued/started/e2e.step，随后 cancel                                                                   |
| Automation         | Run now 产生 queued run history；pause 显示 PAUSED，resume 回到 ENABLED；任务列表立即刷新                                                                                                                                       |
| Connectors         | 创建 stdio connector 时默认 DISABLED，启用后 READY；页面明确不接收 Secret，认证仅允许 API 环境                                                                                                                                  |
| Permissions        | Host 为 read-only/never；Waker policy 从 allow 收紧到 tool/file deny 与空 builtin tools；页面明确 codex-host 才是执行者，浏览器不能扩大权限                                                                                     |
| Human Actions      | 创建明确标识“本地演示”的 pending workflow action，处理后 pending 数从 1 变 0                                                                                                                                                    |
| Project context    | 新会话选择“Ego 公开项目”，真实 Kimi 回复成功；`session_contexts` 回读 projectId 与受控 workingDirectory                                                                                                                         |
| Attachment → Codex | 浏览器上传 `attachment-evidence.txt`，勾选用于下一轮；Codex 只回答文件中的 `EGO_ATTACHMENT_TOKEN_2026`，证明附件通过服务端受管存储进入 SDK turn                                                                                 |
| Session outputs    | 同一附件登记为 Artifact，并登记 `docs/validation/attachment-evidence.txt` file change；面板回读附件/结果/变更各一条                                                                                                             |
| Stop               | 长回答运行时出现可访问的“停止生成”按钮；点击后会话显示“回复已中断/继续”，没有丢失前序消息                                                                                                                                       |
| Knowledge 运维     | 扩展 UI 显示编辑/删除、binding/audit 计数；Rebuild 返回“已重建 2 个分块”                                                                                                                                                        |
| Waker 生命周期     | 实际页面提供搜索、Markdown 导入、服务端 source 导出、名称确认删除、配置、记忆与能力入口                                                                                                                                         |
| 复杂移动页         | Knowledge 页面 390×844，`scrollWidth=390`、无横向溢出                                                                                                                                                                           |
| 最终错误收口       | Runtime exception、console error、非预期 loadingFailed、HTTP ≥400 汇总为 `count=0`                                                                                                                                              |

第二轮持久化回读：

```text
memory.sqlite: documents=2 versions=4 snapshots=3 timeline=4
workspace.sqlite: automation_runs=1 workflow_runs=2 workflow_events=9 connectors=1 permission_policies=1 human_actions=1 session_contexts=1
artifacts/artifacts.sqlite: attachments=1 artifacts=1 file_changes=1
```

## P0 本地管理闭环第三轮

- Ego Lite task space：`1`，桌面与 390×844 全程复用；验收完成后 `completeTaskSpace(1, { keep: false })` 返回 `done: true`。独立 finish review 修复后，因原 space 已关闭，新建确认 space `3`，最终同样关闭并返回 `done: true`。
- 本轮使用 `Ego P0 Waker` 作为持久化验证 Waker；移动临时 Waker 通过名称确认删除，最终交付前也移除 P0 测试 Waker 定义，不把验收角色混入默认产品配置。

| 范围                 | 实测结果                                                                                                                                                                                  |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Waker 创建引导       | 桌面创建 `Ego P0 Waker` 后仍停留在管理页，焦点回到“新建 Waker”，页面显示 Chat、Knowledge、Project 三个真实下一步入口；390px 再次创建验证三入口单列、`scrollWidth=390`，随后清理临时 Waker |
| Project 路径错误     | 创建 private/filesystem Project 时提交 `../outside`，服务端返回“项目路径不存在或不可读取”，对话框与输入保持；相关单测另覆盖外部绝对路径和 symlink 逃逸                                    |
| Project 生命周期     | `docs` private/filesystem 创建成功，编辑为 public/local Git checkout `.` 成功；删除影响显示 session contexts=0、tasks=0、磁盘目录不删除，名称确认后删除成功                               |
| Project 持久化       | 创建 `Ego P0 持久项目`：private/filesystem/`packages`；刷新、重新选择 Waker 后详情、权限和路径保持；其他 Waker 的公开项目在 390px 显示“只读”，编辑禁用                                    |
| Knowledge 已有库     | “Waker 本地指南”执行 connect→hybrid search→unbind；检索返回 2 条引用，包括 `docs/local-guide.md#L1-L5`                                                                                    |
| Knowledge 新建与导入 | 创建并自动连接 `Ego P0 导入库`；上传 `p0-knowledge-import.md` 显示“已导入 1 个，失败 0 个”；上传 PNG 显示“已导入 0 个，失败 1 个”并进入 Needs check                                       |
| Knowledge 重建与检索 | 重建返回 1 chunk，状态恢复 Connected；检索 `EGO_P0_KNOWLEDGE_TOKEN_2026` 返回 1 条 hybrid 结果及引用 `p0-knowledge-import.md#L1-L6`                                                       |
| Knowledge 绑定状态   | 新库解绑后以 read-only 重绑；状态显示“只读”，导入与新建文档均禁用；刷新后绑定与文档仍在                                                                                                   |
| 响应式与输入         | Project、Knowledge 与 Waker 引导在 390×844 均 `scrollWidth=390`；管理表单输入计算字号为 16px，避免移动浏览器聚焦缩放                                                                      |
| Finish review 修复   | Knowledge 切换立即清空旧文档并显示 loading；只读/Connected 与 Needs check 分离；文档删除增加影响确认；notebook 有 `aria-pressed`；390px 关键操作目标实测均为 44px                         |
| 故障与恢复确认       | space `3` 用 CDP 阻断 documents 请求后显示 `Connected + Needs check`、旧文档为空和“重新读取”；解除阻断后重试恢复 2 篇文档及单一 Connected 状态                                            |
| Console/Network      | 最终 ResourceTiming HTTP ≥400 为 `[]`；Ego 事件队列仅有正常 `Target.attachedToTarget`，无 runtime exception、console error 或 loadingFailed                                               |

第三轮持久化回读：

```text
workspace.sqlite: Ego P0 持久项目 = private/filesystem/packages/ready，owner=ego-p0-waker
knowledge.sqlite: Ego P0 导入库 documents=1 chunks=1 embeddings=1 audits=6
binding: waker/ego-p0-waker/read_only
embedding: local-hash-v1, dimensions=64；document current_version=1；citation lines=1-6
```

## Goal 2：附件与结果体验

- 主要 Ego Lite task space：`5`。该 space 在收口前由 Ego Lite 清理且 `listTaskSpaces()` 回读为空；随后新建只读确认 space `6`，最终 `completeTaskSpace(6, { keep: false })` 返回 `done: true`。
- 真实成功模型：`Kimi K2.7 Code`。默认 OpenAI 模型先后出现一次 TLS EOF 与一次 401，均保留待发送附件；切换已配置 Kimi 后在同一会话成功，不把外部凭据错误记为附件失败。

| 范围                  | 实测结果                                                                                                                                                |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 首轮 Composer         | 文件选择加入 `attachment-goal2.txt` 与 390×844 PNG；拖放批次保留 `batch-good.txt` 并单独拒绝 `.env`；图片缩略图自然尺寸 390×844，页面无溢出             |
| 失败保留              | TLS/401 两次模型失败后 TXT/PNG 仍在待发送列表；快速移除额外文件后即时发送只携带剩余项；自动化另覆盖 Welcome→Thread 分支切换失败保留                     |
| 真实首轮 turn         | Kimi 从文本附件返回 `GOAL2_FIRST_TURN_ATTACHMENT_TOKEN_2026`，从图片返回 `390x844`；done 后待发送列表自动清空                                           |
| 已有附件 turn         | Outputs 全选 3 个已有附件后发送，Kimi 返回 `OUTPUTS_GOOD_TOKEN_2026`；成功后选择归零                                                                    |
| Batch partial success | Outputs 拖放 `outputs-good.txt + .env`，页面显示 1 成功/1 失败；成功文件可预览 `OUTPUTS_GOOD_TOKEN_2026`，失败显示 ArtifactStore 的敏感文件名拒绝       |
| 文本/图片预览         | TXT 以转义文本显示；PNG 缩略图和 object URL 大图均为 390×844；预览打开焦点进入“关闭预览”，Escape 后恢复触发器                                           |
| 下载/结果/变更        | PNG 下载返回 200、`image/png`、59448 bytes；文本附件登记 Artifact，登记 file change 及摘要后均展示路径和时间                                            |
| 删除与引用计数        | 删除已登记 Artifact 的源附件后，附件行消失但 Artifact 保留；SQLite 指向 `blobs/bb/...`，磁盘文件仍存在且为 158 bytes                                    |
| 刷新恢复与隐私        | 刷新后 3 个附件、Artifact、file change 均回读；会话标题仅为用户问题，rollout 的 `<image path="/Users/...">` transport marker 已过滤，页面无主机路径泄露 |
| 390px                 | Outputs overlay 宽 358.8px、停在 62px 底栏上方；关键操作目标前 10 个均为 44px；预览对话框 358px，无横向溢出                                             |
| Console/Network       | 最终确认 space `6` 中 Runtime/Console/Network/HTTP ≥400 均为 `[]`                                                                                       |

持久化回读：

```text
session=session_e3abefde
attachments=3: image/png 59448 bytes + two text/plain
artifacts=1: attachment-goal2.txt -> blobs/bb/bb5b... (source attachment deleted)
file_changes=1: docs/validation/attachment-goal2.txt / update
artifact managed file exists: 158 bytes
```

## Goal 3：Chat 阅读体验

- Ego Lite task space：`7`（本轮独立重新分配）。真实验收完成后，专用最终 heredoc 调用 `completeTaskSpace(7, { keep: false })` 返回 `done: true`。
- 真实模型：`Kimi K2.7 Code`。验证会话 `session_fd7302b1` / Codex thread `01a048da-755c-7611-bed2-d68a429bc503`。

| 范围                 | 实测结果                                                                                                                                                                                                                                           |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 真实长回答           | 提问要求读取 `package.json`、检索 `EGO_P0_KNOWLEDGE_TOKEN_2026`、返回 TypeScript fenced code 与 26 行清单；Kimi 返回 `packageManager=pnpm@10.30.3`、目标 token、代码块与完整清单，完成态自动折叠                                                   |
| 长内容可访问性       | 收起态 `aria-expanded=false`、内容节点带 `inert + aria-hidden=true`，屏幕阅读器保留 320 字摘要；对隐藏代码按钮调用 `focus()` 不会进入裁切区域，展开后原始 Markdown 完整恢复                                                                        |
| 代码复制/下载        | `typescript` 语言条可见；点击复制显示“已复制”；下载捕获 `snippet.ts` 与 `blob:https://waker.localhost/...`，操作完成后再次 fetch 该 URL 失败，证明 object URL 已 revoke；移动按钮为 51×44px                                                        |
| 计划与工具           | 流式中出现三步 plan，状态由 `0/3 → 2/3 → completed`；完成时 4 个过程。刷新回放后 5 个去重过程（2 个 `update_plan`、3 个 command execution）全部显示“已完成”，绝对路径未进入页面                                                                    |
| 结构化来源与定位     | SSE/SQLite 保存 2 个 hybrid 来源；来源面板展示 notebook/document/chunk、版本、score、行范围与转义摘录。正文中属于结构化来源的 `[2]` 变成“查看来源 2”，点击后 details 打开、焦点进入 `citation-rollout_3-2`；模型自述但无 DTO 的 `[3]` 不伪装成链接 |
| Outputs 联动         | 点击消息尾部“查看附件与结果”打开当前 Session panel，空附件/结果/文件变更状态真实显示；不会把 command/file 操作伪造成 Artifact                                                                                                                      |
| 刷新与 SQLite 回读   | 整页 reload→展开会话栏→重开最新会话后，原始 user query、2 个来源与 5 个过程恢复。`session_turn_sources` 为 `(session_fd7302b1, turn=1, sources=2)`；messages API 回读 `count=3/sourceCount=2/toolCount=5/pathLeak=false`                           |
| 390px Chat           | 主内容 `width=390`、document `scrollWidth=390`；收起会话栏为固定 44×44 trigger 且不占 flex，展开 drawer 为 319.8×782、主内容仍 390、止于 62px 底栏。Header 自身横向滚动，按钮不再压成竖排；过程/代码/复制目标均 ≥44px                              |
| 安全边界             | 来源持久化为 host-owned SQLite sidecar，用户文本无法伪造；知识/项目/附件 JSON 中 `<>&` 编码，URI credentials/query/主机绝对路径不进入模型引用或浏览器；失败 turn thinking/usage 不串到下一轮，runtime/provider error 在 SSE 前脱敏                 |
| Console/Network/HTTP | 最终 reload 启用 Runtime/Log/Network 后捕获 744 个事件，其中 222 个相关事件；runtime exception、error log、loadingFailed、HTTP 4xx/5xx 汇总 `bad=0`                                                                                                |

持久化回读：

```text
workbench.sqlite session_turn_sources: session_fd7302b1 / turn_index=1 / source_count=2
messages API: user query is clean XML-decoded text; final assistant sourceCount=2 toolCount=5
SDK boundary: @openai/codex-sdk imports remain under packages/codex-runtime only
```

## Goal 4：Automation 本地调度与真实运行

- Ego Lite task space：`8`，桌面与 390×844 全程复用；验收完成后专用 heredoc 调用 `completeTaskSpace(8, { keep: false })` 返回 `done: true`。
- 真实模型：`Kimi K2.7 Code`。本轮显式保留三条定义，便于本地复查 interval/manual、once/scheduled 与 cron/timezone 状态。

| 范围                  | 实测结果                                                                                                                                                                                                                                 |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 创建与字段错误        | UI 创建 `Goal4 手动暂停验证`：`interval:600000`、Asia/Shanghai、maxRuns=2、local-project、Kimi、medium；把 timezone 改成 `Mars/Olympus` 后编辑器原地显示 `Invalid timezone`，输入和值保持，改回合法值后保存成功                          |
| Paused manual         | 暂停后状态“已暂停”、next run 清空，但“立即运行”仍可用；真实 Run `3805f325-...` 创建独立 Session `session_1d493bfc`，返回 `AUTOMATION_MANUAL_GOAL4_2026` 与 usage `13355/413/13768`，定义保持 paused                                      |
| Scheduled once        | UI 创建 `Goal4 定时验证`：`once:1787933338554`、Asia/Shanghai、maxRuns=1、Kimi；本地 scheduler 到期自动 claim，状态 running→succeeded，run `a367c1b9-...` 的 trigger=scheduled/scheduledFor 精确，返回 `AUTOMATION_SCHEDULED_GOAL4_2026` |
| Cron / pause / resume | UI 创建 `Goal4 Cron 验证`：`*/15 * * * *`、Asia/Shanghai、maxRuns=3、misfire=skip；resume 后 next run=`2026-08-29 00:15`，再次 pause 后清空，证明 cron 不再只是校验占位                                                                  |
| Cancel / retry        | 手动长任务 run `660ec3f3-...` 运行中出现“取消运行”，取消后为 cancelled 且保留 Session `session_a84e567b`；点击重试生成新 run `0e0949b5-...`、attempt=2、retryOf 指向原 run、新 Session `session_419d6e43`，最终 succeeded                |
| Session 导航与隐私    | 从运行详情打开 Automation Session；user history 仅显示原 prompt，不显示 `data-waker-host="automation-v1"`，assistant 回答恢复；结果、错误和页面均无 `/Users/...` 主机路径，provider key 不进入浏览器                                     |
| 历史与只读 API        | 运行详情展示 manual/scheduled、时间、duration、attempt、project/model/thinking、result、usage/error 和 Session；分页 API `limit=2` 返回 `total=3`。旧 client-forge `start/complete/fail` 三个 endpoint 均为 404                          |
| SQLite 与迁移         | 真实 `.codex/workspace.sqlite` 在不删数据下由早期 004 原地升级到 005；PRAGMA 确认 definition context 列与 deleted-aware due index。三条定义、4 条真实 run、4 个独立 Session/context 及 immutable snapshot/usage 与 UI 一致               |
| Project/Agent 安全    | 自动化只列 owned project；Project 删除影响包含 definition/run/preserved task，active run 返回 409，历史 task/run 不被 FK cascade 删除；Agent 删除期间 Automation 写入返回 409，并在异步清理后复核软删除                                  |
| 390px                 | Automation manager=346px、workspace=346px、document `scrollWidth=390`；定义列表横向滚动、详情单列，输入字号 16px；Run/Edit/Delete 等关键按钮均 44px；页面无主机路径泄露                                                                  |
| Console/Network/HTTP  | 验收中预期一次 invalid timezone 400，load generation 主动取消产生两次 `ERR_ABORTED`；清空这些已解释事件后等待稳定 poll，最终 Runtime/Log/Network 队列 `events=0, bad=0`                                                                  |

持久化回读：

```text
manual success: run=3805f325... session=session_1d493bfc result=AUTOMATION_MANUAL_GOAL4_2026
scheduled success: run=a367c1b9... session=session_26719a42 scheduled_for=1787933338554
cancel -> retry: 660ec3f3... cancelled -> 0e0949b5... attempt=2 succeeded / session=session_419d6e43
definitions: interval paused; once completed run_count=1/1; cron paused Asia/Shanghai misfire=skip
```

## Goal 5：Workflow 版本化编排与真实执行

- Ego Lite task space：`9`。
- 本轮遵循当前 `AGENTS.md` 的 PC desktop-only 前端范围，只验收桌面工作面，不新增移动端断点或移动交互验收。
- 最终 package-scope 计数为 Workspace 30、API 97、Web 148、Codex Runtime 103；连同 Knowledge 7、Memory 5、Artifacts 8，monorepo 共 398 项测试通过。

| 范围                    | 实测结果                                                                                                                                                                                                                                          |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Strict JSON 与路径图    | Workflow 定义以服务端校验的 JSON 为唯一可执行来源；支持 `action`、`codex`、`decision`、`wait`、`ask_user`、`call_workflow`、`terminal` 七种节点。桌面详情同步显示只读路径图，不把它伪装成可拖拽 diagram editor                                    |
| 版本 / diff / rollback  | 从 v1 编辑到 v2 后可查看 immutable version diff；dry-run rollback 先展示回退内容，apply 后不是覆写历史，而是创建 v3。版本冲突由 expected version 拒绝，定义与版本历史刷新后仍一致                                                                 |
| 人工输入真实运行        | Run `b953feb3-...` 创建独立 Session `session_8d43509a`；执行器进入 `waiting_input` 并生成 Human Action，桌面提交答案后恢复，同一 Run 最终 succeeded，结果为 `WAKER_WORKFLOW_GOAL5_OK`                                                             |
| Codex provider 失败诚实 | 首次按默认 OpenAI 模型执行时 provider 返回 401，Run 如实进入 failed 并保留错误/Trace，没有浏览器 forge `complete`。将定义模型改为已配置的 `kimi-for-coding` 后重新运行成功                                                                        |
| 真实 Codex Run          | Kimi Run `244f1aad-...` 使用独立 Session `session_fb1280e1`，最终返回 `WAKER_WORKFLOW_CODEX_GOAL5_2026`；usage 为 input `13364`、output `79`、total `13443`，结果、Session 与有序 Trace 在刷新后仍可回读                                          |
| Durable executor        | owner-scoped executor 由宿主推进 queued/running/waiting/terminal 状态；每个 Run 仅绑定一个 Codex Session。自动化覆盖 action/decision/wait/ask_user/call_workflow/terminal、restart recovery、cancel/retry、Human Action 原子恢复与最大 8 层子流程 |
| 历史与安全边界          | 定义列表、版本与 Run 历史使用 bounded 查询；浏览器只能请求 run/resume/cancel/retry，不能 append/complete/fail 伪造生命周期。Prompt 的 host wrapper 不进入用户会话历史，页面不返回 provider key 或主机绝对路径                                     |

持久化回读：

```text
manual ask: run=b953feb3... session=session_8d43509a waiting_input -> succeeded result=WAKER_WORKFLOW_GOAL5_OK
versions: v1 -> v2 -> rollback-created v3
codex: default OpenAI -> failed(401); kimi-for-coding run=244f1aad... session=session_fb1280e1 -> succeeded
codex result: WAKER_WORKFLOW_CODEX_GOAL5_2026 usage=13364/79/13443
```

## Goal 6：Board 与 Skills

- 本轮遵循 `AGENTS.md` 的 PC desktop-only 范围，不新增移动端验收项。
- Ego Lite 已完成 Board Lanes、真实 Workflow Human Action、Skill upload/readback/conflict/remove 的桌面验证；最终刷新后事件队列未出现 Console/Network/HTTP 错误。
- 最终门禁：install/build/typecheck/lint/test/Prettier/diff 全部通过。Workspace 34、Codex Runtime 101、API 101、Web 159，加 Knowledge 7、Memory 5、Artifacts 8，共 415 tests、`fail=0`。

| 范围                  | 实测结果                                                                                                                                                                                                                                                                                     |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Board List/Lanes      | 同一 server-filtered Task 集可在 List/Lanes 间切换；Automation/Workflow derived Task 保持 managed/read-only，状态、Session、result 与 source navigation 来自宿主 Run。桌面 Lanes 截图已保存                                                                                                  |
| Workflow Human Action | `Goal 6 Board 人工操作` 的 Run `5c65601a-...` / Task `7650d7e1-...` / Action `ff4ea4c9-...` 进入 waiting；在 Board 输入 `GOAL6_RESUME_OK` 后，Action handled、Workflow succeeded、derived Task completed，三者共用 Session `session_46817acf`，Task result 原样回读为该输入                  |
| Task timeline         | 该 derived Task 持久化 8 个有序事件：created、workflow.started、session_linked、waiting、human_action.created、workflow.resumed、human_action.handled、workflow.succeeded；最终 status 为 completed                                                                                          |
| Manual/derived 边界   | 自动化覆盖 manual Task expectedVersion CAS、stale conflict、软删影响、bounded paging，以及 Automation/Workflow projection 的只读同步；旧 browser `/api/v1/tasks` forge 写入口仍不可用                                                                                                        |
| Migration 008/009/010 | 真实 `.codex/workspace.sqlite` 的 `schema_migrations` 为 001–010。008 建立 manual/derived Task、run/action 关系与 timeline；已应用的 008 保持冻结，009 前向补 `tasks.description` 并回填 Workflow snapshot，010 前向补 `human_actions.session_id` 并由关联 Task/Run 回填                     |
| Skills runtime scope  | Skills 是 workspace-shared，不是 per-Waker。`.agents/skills` 是 repo scope；项目 `CODEX_HOME` 下 `.codex/skills` 是 Waker host runtime scope，两者 valid 时都为 `available`。稳定 locator 区分同名 scope，详情展示 frontmatter、source/lock/version、policy、dependencies/files 与 integrity |
| Skill upload/readback | 通过 UI 上传 instruction-only `goal6-ego-skill`，页面显示 repo locator 与有效 metadata，详情正文成功回读；截图 `goal6-desktop-skills-uploaded.png` 保存上传后的真实状态                                                                                                                      |
| Skill conflict/remove | 对同名 Skill 上传不同内容得到可见 conflict，未覆盖已安装定义；确认删除后 `.agents/skills/goal6-ego-skill`、对应 `.codex/skill-sources` staging 与 `skills-lock.json` entry 均无残留                                                                                                          |
| 第三方与官方边界      | Repo mutation 使用 pinned `skills@1.5.23` CLI；host source 仅按 exact locator 管理。skills.sh 仍是未审查第三方发现源。按[官方 Codex Skills 文档](https://developers.openai.com/codex/skills)，上下文先加载 name/description，选择后才完整加载 `SKILL.md`；本项目不声称 per-Waker 绑定        |

## 视觉证据

- 桌面截图：`docs/validation/screenshots/desktop.png`
- 移动截图：`docs/validation/screenshots/mobile.png`
- 扩展桌面截图：`docs/validation/screenshots/extended-desktop.png`
- 扩展移动截图：`docs/validation/screenshots/extended-mobile.png`
- 最终桌面截图：`docs/validation/screenshots/final-desktop.png`
- 最终移动截图：`docs/validation/screenshots/final-mobile.png`
- P0 桌面 Project：`docs/validation/screenshots/p0-desktop-project.png`
- P0 桌面 Knowledge：`docs/validation/screenshots/p0-desktop-knowledge.png`
- P0 移动 Knowledge：`docs/validation/screenshots/p0-mobile-knowledge.png`
- Goal 2 桌面 Outputs：`docs/validation/screenshots/goal2-desktop-outputs.png`
- Goal 2 移动 Outputs：`docs/validation/screenshots/goal2-mobile-outputs.png`
- Goal 3 桌面来源：`docs/validation/screenshots/goal3-desktop-chat.png`
- Goal 3 桌面过程：`docs/validation/screenshots/goal3-desktop-process.png`
- Goal 3 移动 Chat：`docs/validation/screenshots/goal3-mobile-chat.png`
- Goal 3 移动来源：`docs/validation/screenshots/goal3-mobile-sources.png`
- Goal 3 移动会话 drawer：`docs/validation/screenshots/goal3-mobile-drawer.png`
- Goal 4 桌面运行历史：`docs/validation/screenshots/goal4-desktop-automation.png`
- Goal 4 桌面 scheduled：`docs/validation/screenshots/goal4-desktop-scheduled.png`
- Goal 4 移动 Automation：`docs/validation/screenshots/goal4-mobile-automation.png`
- Goal 5 桌面 Workflow 定义/版本：`docs/validation/screenshots/goal5-desktop-workflow.png`
- Goal 5 桌面 Workflow Trace：`docs/validation/screenshots/goal5-desktop-workflow-trace.png`
- Goal 5 桌面 Codex 成功运行：`docs/validation/screenshots/goal5-desktop-codex-success.png`
- Goal 6 桌面 Board Lanes：`docs/validation/screenshots/goal6-desktop-board-lanes.png`
- Goal 6 桌面 Board 完成态：`docs/validation/screenshots/goal6-desktop-board-completed.png`
- Goal 6 桌面 Skill 上传回读：`docs/validation/screenshots/goal6-desktop-skills-uploaded.png`
- 对话框焦点实测：打开后焦点进入输入框，`Escape` 关闭后回到原删除按钮；对应 `useDialogFocus` 自动化测试也已通过。
- Finish reviewer 最终 disposition：`ship`
- 机械 detector 因本机缺少 HTML parser 依赖退化为 regex 模式并返回 `[]`；因此视觉结论以真实 Ego Lite 桌面/移动/深色渲染和 finish reviewer 为主，不把 detector 当 computed-style 证明。
