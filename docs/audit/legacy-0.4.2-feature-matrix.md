# QoderWake 0.4.2 前端功能矩阵

本文档是目标仓库复现旧版前端的范围基线。审计对象为：

- 产品构建：`/Users/xbjt/Documents/myself/waker-source/qoderwake-source-archive/versions/0.4.2-cn-6eeb338baf65/web-assets`
- 可读服务端恢复源码：`/Users/xbjt/Documents/myself/waker-source/qoderwake-source-archive/versions/0.4.2-cn-6eeb338baf65/src/daemon`

证据来自实际路由、UI 字符串、CSS、静态资源和服务端路由，不从文件名推断行为。旧版为生产构建且无 source map，主 bundle 中的定位使用字节偏移；行号仅用于恢复出的服务端源码。

状态定义：

- `verified`：当前本地实现已经过 Ego Lite 真实浏览器流程验证，并有页面状态、网络/控制台或持久化回读证据。
- `implemented`：当前代码与自动化测试已覆盖主要本地语义，但尚无完整 Ego Lite 流程证据，或浏览器证据未覆盖全部分支。
- `partial`：只实现了旧版能力的一部分；未实现部分仍影响该行完整语义，不能按完成宣称。
- `degraded`：以明确标识的本地替代或禁用状态承接专有云、企业服务或外部 provider 能力，不伪造成功。
- `excluded`：经范围决策明确不实现，且不属于本地成功主路径。

状态按整行能力判定：即使其中一个动作已通过验收，只要同一行仍包含未实现的关键语义，该行仍标为 `partial`。自动化测试全绿只证明被测试的合同，不自动提升为 `verified`。

## 证据索引

| 证据             | 位置                                                   | 可证明内容                                                                                                           |
| ---------------- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| Web 入口         | `web-assets/index.html`                                | React 根节点、QoderWake 标题、主 JS/CSS 构建入口                                                                     |
| 主前端 bundle    | `web-assets/assets/index-uBX0Yksq.js`                  | App Rail 约偏移 328583；全局设置导航约 590212；Waker 二级导航约 609146；知识绑定约 2705844；完整 Router 表约 5195385 |
| 主样式           | `web-assets/assets/index-DQ5SLFCf.css`                 | light/dark tokens、reduced-motion、触控/hover 和 480–1380px 等响应式断点                                             |
| 知识库 UI        | `web-assets/assets/KnowledgePackageRoutes-D627TbgJ.js` | 知识库列表、created/featured 详情路由和管理界面                                                                      |
| 核心 HTTP API    | `src/daemon/modules/WebServer.ts:11085`                | Waker、会话、事件流、设置、记忆、Skills、Connectors、Permissions、Projects 等接口                                    |
| Conversation API | `src/daemon/conversations/ConversationsHttpApi.ts:56`  | Conversation、消息、附件、成员、群组、线程和工作区绑定                                                               |
| Knowledge API    | `src/daemon/modules/qmind/QmindRouter.ts:15`           | notebooks、sources、cards、notes、chat、compilation、bindings                                                        |
| Trigger API      | `src/daemon/modules/TriggerRouter.ts`                  | Automation CRUD、运行记录、Run now、来源探测、Webhook                                                                |
| Workflow API     | `src/daemon/modules/WorkflowRouter.ts`                 | Workflow CRUD、版本、运行、暂停、取消、重试和人工确认                                                                |
| 当前实现         | `apps/`、`packages/`                                   | 当前 Web/API、Codex runtime、knowledge、memory、artifacts、workspace-data 实现与测试                                 |
| Ego Lite 验收    | `docs/validation/ego-lite-2026-08-28.md`               | 两轮浏览器操作、持久化回读、桌面/移动/深色截图、控制台与网络错误收口                                                 |

## 页面与能力矩阵

| 页面/区域                            | 能力                                                                                                  | 优先级 | 旧版证据                                                                                                   | 当前本地实现与验证结论                                                                                                                         | 状态        |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| App Shell                            | 极窄全局图标轨、上下文二级侧栏、主内容区、激活态                                                      | P0     | bundle `Zae`：Wakers、Chat、IM、WakerFlow、Board、Public projects、Knowledge；`qc-app-shell`/`qc-app-rail` | 八个主入口可达；桌面 64px rail、移动底栏与激活态已由 Ego Lite 验证                                                                             | verified    |
| App Shell                            | 浅色/深色主题、触屏/hover、reduced motion、多档响应式                                                 | P0     | `index-DQ5SLFCf.css` 的 `data-theme`、`.dark`、`prefers-reduced-motion` 和宽度断点                         | 共享 tokens、390px 无溢出与深色模式已验证；reduced-motion 有 CSS/组件实现但未单独截图验证                                                      | implemented |
| Waker 管理 `/management`             | Waker/群组列表、搜索、设备与在线状态筛选、分页、未读                                                  | P0     | `management.*`：`searchWorkersOrDevices`、environment/availability filters、mark all read                  | 本地 Waker 列表、搜索和生命周期入口已验证；群组、设备筛选、分页与未读语义未完整复现                                                            | partial     |
| Waker 管理                           | 空列表、无搜索结果、loading、局部加载失败、retry                                                      | P0     | `emptyWorkers*`、`noWakersMatchFilters`、`loadingWorkers`、`loadFailed`                                    | 页面具备 loading/empty/error/retry；项目页注入失败与恢复已验证，但未逐个异步区域验收                                                           | implemented |
| 创建 Waker `/wakers/new`             | 模板或自定义创建，头像、名称、角色描述、运行环境、Skills                                              | P0     | `worker.create.*`；Router `/wakers/new`；`POST /api/agents`                                                | 名称/描述/id 创建、持久化与 Chat/Knowledge/Project 三入口成功引导已由 P0 Ego 验证；头像、运行环境与创建时 Skills 仍不完整                      | partial     |
| Waker Home                           | 画像编辑、能力/工具、活动、项目/触发器/任务统计、成长记录                                             | P0     | `worker.home.*`：about、work styles、heatmap、counts、timeline/history                                     | Waker 卡片、配置、记忆、能力和本地资源入口已实现；热力图、成长记录与完整活动统计缺失                                                           | partial     |
| Waker 设置                           | 编辑 `IDENTITY.md`、`PERSONA.md`、`BIBLE.md` 与 profile                                               | P1     | bundle `gXe`；`GET/PUT /api/agents/:agentId/context/:fileName`                                             | 可编辑/导入/导出单一 Markdown Waker 定义；尚未拆成三个旧版上下文文件与结构化预览                                                               | partial     |
| Waker 本地生命周期                   | Markdown 导入、source 导出、名称确认删除、配置/记忆/能力入口                                          | P0     | `POST /api/agents`、export-package、delete-impact 与 management 生命周期入口                               | 搜索、Markdown 导入、服务端 source 导出、名称确认删除和配置/记忆/能力入口均由第二轮 Ego Lite 验证                                              | verified    |
| Waker 云分享                         | 分享链接、过期与撤销、链接/ZIP 导入、云端影响分析                                                     | P2     | `delete-impact`、`export-package`、`share-link` 接口及 `management.share*`                                 | 本地版本不提供云分享；本地 Markdown import/export 是替代路径，不能视为云分享成功                                                               | degraded    |
| Waker 详情导航                       | Home、Projects、Automations、Tasks、Workflows、Memories、Skills、Knowledge、Connector、IM、Permission | P0     | bundle `kme` 直接列出 label/path；Router `/wakers/:workerId/*`                                             | 主 rail 与 Waker 卡片入口覆盖项目、自动化、任务、流程、记忆、能力；部分旧版二级页面合并呈现                                                    | implemented |
| 新任务 `/conversations/new`          | 选择 Waker、项目、模型、工作目录；输入任务并发送                                                      | P0     | `new-task-page.png`；`conversation.inputPlaceholder`；session reservation/create 接口                      | Waker/模型/项目选择、受控 workingDirectory 和真实 Codex SDK turn 已由 Ego Lite 验证                                                            | verified    |
| Composer                             | 多行输入、图片/文件、拖放、粘贴文本、附件预览与错误                                                   | P0     | `conversation.addFile`、`droppedFile*`、`fileAttachFailed`、`pastedTextAttachment*`                        | 新会话首轮/后续文本与图片、选择/拖放/粘贴文件、缩略图、逐文件错误、失败保留与 8 个合计上限已由测试和 Goal 2 Ego 验证                           | verified    |
| 对话 `/conversations/:sessionId`     | 流式回复、停止/中断、等待审批、等待用户问题、失败重试                                                 | P0     | `statusStreaming`、`statusWaitingApproval`、`statusWaitingQuestion`、`replyInterrupted`；`events/stream`   | SDK Thread SSE、thinking/text/done、停止和刷新恢复已验证；审批/提问通过 Human Actions 承接，未验证原 Thread 恢复                               | partial     |
| 对话内容                             | Markdown、代码、Mermaid、工具调用、计划卡、长消息折叠                                                 | P0     | Mermaid/代码语言 chunks；`buildPlan`、`viewPlan`、`toolFailed`                                             | GFM、安全外链、代码语言/复制/下载、可访问长消息折叠、四态计划/工具卡及结构化来源面板已实现并可回放；Mermaid 与语法高亮未复现                   | partial     |
| 会话历史                             | 列表、搜索、分组、未读、加载更多、重命名、删除、刷新恢复                                              | P0     | `console-sessions` query/read-all/PATCH/DELETE；`noSessions`、`loadOlderFailed`                            | SQLite 绑定、rollout 回放、重命名/删除/收件箱与 Thread resume 已测试；刷新恢复已由 Ego Lite 验证                                               | verified    |
| 对话结果                             | Artifacts、最终文件、file changes、下载、打开路径、远程上传状态                                       | P1     | `conversation.artifact*`；session artifacts/file-changes 接口                                              | 批量 upload/partial-success、文本/图片预览、下载/删除、Artifact/file change 详情和跨表引用计数已由 Goal 2 验证；远程上传明确不提供             | verified    |
| 对话错误                             | 启动、网络、鉴权、超时、rate limit、quota、无模型                                                     | P0     | `startupFailed.*`、`workerError.*`、`modelEmpty`、network diagnostic                                       | stop/interrupted 与注入网络失败后的 retry 已验证；鉴权、超时、rate limit、quota 分类恢复未逐项验证                                             | partial     |
| Conversation Groups                  | 群聊、成员、leader、group/thread settings、conversation skills                                        | P2     | `/conversations/groups/*`、`waker-groups/*`；`ConversationsHttpApi.ts:453` 起                              | 本地首版未实现多 Waker 群聊；不属于成功主路径                                                                                                  | excluded    |
| Projects                             | 私有项目 CRUD、详情、重试初始化、local/Git/remote source                                              | P0     | `/wakers/:id/project`；`project.*`；`/api/v1/agents/:agentId/projects`                                     | public/private、filesystem/local Git checkout、详情、编辑、只读公开项目、删除影响、受控 workingDirectory 与刷新持久化已验证；不做 remote clone | verified    |
| Public Projects                      | 公共项目、成员、只读画像和删除                                                                        | P2     | `/projects/public/:projectId`；`/api/v1/public-projects*`                                                  | 以本地 `visibility=public` 替代并已验证创建；无多用户成员/云公共画像语义                                                                       | degraded    |
| Knowledge `/knowledge`               | created/shared/featured 列表、搜索、创建、删除、详情                                                  | P0     | Knowledge routes；`QmindRouter.ts:65` 起；`worker.knowledge.modal.tabs.*`                                  | 本地 notebook 创建/列表与文档管理已实现；旧版 shared/featured 分类未完整呈现                                                                   | partial     |
| Knowledge 详情                       | Source 列表与详情、上传、Markdown、Web link、预览、重命名、删除                                       | P0     | Qmind sources routes；`Source already exists`、upload feedback/result strings                              | Markdown/TXT 批量导入、大小/类型/空/二进制限制、逐文件 partial-success、编辑/删除与引用已验证；远程 URL 抓取和富预览未实现                     | partial     |
| Knowledge 检索                       | 关键词、向量、混合检索、去重排序与引用                                                                | P0     | 旧 UI 体现 Waker 检索绑定与 knowledge references；旧后端依赖 QMind                                         | SQLite FTS5、LocalHash vector、hybrid 去重与行号引用三种模式均由 Ego Lite 和测试验证                                                           | verified    |
| Knowledge 运维                       | 增量重建、删除一致性、版本/审计、失败重试                                                             | P0     | compilation/task-runs/action-audits/proposals API                                                          | 文档版本、审计、强制/增量重建、级联删除与 embedding 失败降级已测试；UI 重建和计数已验证                                                        | verified    |
| Waker Knowledge                      | 绑定/解绑；选择现有或创建并绑定；Connected/Needs check                                                | P0     | bundle 约偏移 2705844；`/api/qmind/bindings/:wakerId`                                                      | P0 Ego 已执行选择已有→连接→检索→解绑、创建并绑定、Needs check→重建、read-only 重绑；SQLite binding/audit 回读一致                              | verified    |
| Waker Knowledge                      | featured read-only、空库提示、loading/error/no results                                                | P0     | `worker.knowledge.*` 完整状态文案                                                                          | 只读 binding 与空/加载/错误/无结果组件已实现；featured 独立产品分类未复现                                                                      | partial     |
| Memories `/wakers/:id/memory`        | All/History；个人、项目、群组范围；分类卡 CRUD                                                        | P0     | `memory.tabs.*`、`memory.all.*`、`memory.atWakerProject.*`                                                 | 独立 SQLite memory CRUD、scope/filter、导入导出已实现；Waker scope 已验证，项目/群组范围未逐项浏览器验收                                       | implemented |
| Memories                             | 完整 MEMORY 文档编辑、冲突处理、timeline、版本、diff、rollback                                        | P0     | memory document/timeline/snapshots/diff/rollback API；`useLatest/keepMine`                                 | v1→v2 编辑、快照、unified diff、dry-run/apply rollback→v3、timeline 与导入已由第二轮验证                                                       | verified    |
| Automations                          | 列表 active/paused、空态；新建/编辑/删除/启停/Run now                                                 | P1     | `/wakers/:id/triggers*`；`triggers.list.*`、`triggers.detail.*`                                            | SQLite CRUD/run history 与 API 已测试；Run now、pause/resume、即时任务刷新已由 Ego Lite 验证                                                   | verified    |
| Automations                          | Schedule：one-time、interval、cron、时区、max runs、end date                                          | P1     | `triggers.editor.schedule*`、cron preset/validation；TriggerRouter                                         | one-time/interval 的 nextRun 与 due scheduler 有确定性测试；cron 仅校验/展示，不伪装已调度，时区/max-runs/end-date 未实现                      | partial     |
| Automations                          | API、Event、GitHub Webhook、Aone/DWS sources                                                          | P2     | `triggers.editor.triggerApi/Event/Github` 与大量 source probe 文案                                         | 保留本地 API/event schema；GitHub、Aone、DWS 探测与 webhook 依赖外部连接，未作为成功路径                                                       | degraded    |
| Skills                               | 内置/市场/上传、安装/启停/删除、编辑、版本/diff/rollback                                              | P1     | `/api/agents/:agentId/skills*`；Router `skill`                                                             | 本地/skills.sh 列表、上传、安装、删除和内容读取已有 API/UI 与测试；启停、版本 diff/rollback 不完整                                             | partial     |
| Connectors/MCP                       | Market、CRUD、启停、工具选择、OAuth/认证错误                                                          | P1     | `/api/agents/:agentId/mcps*`、`connector-market`、MCP auth routes                                          | 本地 stdio/HTTP CRUD、启停、工具元数据与 secret 拒绝已测试；创建 disabled→READY 已由 Ego Lite 验证                                             | verified    |
| 专有 Connectors                      | Microsoft 365、Computer Use、Aone/DWS                                                                 | P2     | 专用 connector routes 和 setup states                                                                      | 不提供伪造连接；页面说明认证只在 API 环境，本地 connector 是替代路径                                                                           | degraded    |
| Permissions                          | Tool/File guard、builtin tools、model security、审批处理                                              | P1     | `/api/agents/:agentId/permissions*`、pending approvals/resolve                                             | Waker policy 只能收紧 host 上限，跨 Waker 隔离与越权拒绝已测试；read-only/never 与 deny UI 已验证                                              | verified    |
| Workflow 列表                        | 创建、AI customize、空态、删除、版本历史                                                              | P1     | `/workflow`、`/wakerflow`；`workflow.create/customization/delete/version*`                                 | Workflow 创建、SQLite CRUD/版本与运行历史已实现；创建已验证，AI customize 未实现                                                               | partial     |
| Workflow 编辑                        | diagram/script 双视图，worker/action/decision/ask-user/call 节点                                      | P1     | `workflow.canvasMode.*`、`diagram.node*`、`edit.scriptEditorLabel`                                         | 提供版本化脚本输入；尚无可视 DAG、节点编辑器和节点/行编译定位                                                                                  | partial     |
| Workflow 运行                        | 参数、queued/running/paused/blocked/waiting/succeeded/failed/cancelled                                | P1     | `workflow.runStatus.*`；WorkflowRouter run/pause/cancel/retry/confirm                                      | queued→running→waiting_input→resume→succeeded、另一次 trace/event/cancel 已由 API 测试与 Ego Lite 验证                                         | verified    |
| Board `/board`                       | Lane/List、状态列、筛选、分页、子任务                                                                 | P1     | `board.views.*`、`board.lanes.*`、board APIs                                                               | 本地任务列表聚合 automation/workflow 来源并即时刷新；Lane、分页、子任务与可分享筛选未复现                                                      | partial     |
| Human Actions                        | 待审批列表、分类/来源/状态筛选、Go handle、ignore                                                     | P1     | `/board/human-actions`；`humanActions.*`                                                                   | workflow/codex 来源、Waker 隔离、resolve/ignore 已测试；本地演示 pending→handled 已由 Ego Lite 验证                                            | verified    |
| IM `/im`                             | Channel CRUD、连接状态、bot/user/AI assistance、conversation 配置                                     | P2     | `im-channel-design.html`、`im.*`、IM routes                                                                | 仅提供明确标识的本地演示 channel；Ego Lite 确认 STOPPED 且未配置第三方，不伪造 provider                                                        | degraded    |
| IM Pairing/@Waker                    | Pairing approve/ignore/batch、连接向导、Waker/team 选择                                               | P2     | `/im/pairings/:pairingId/:tab?`；`im.pairing.*`、`im.atWakerPage.*`                                        | 未实现真实 pairing 与外发消息，不属于本地成功主路径                                                                                            | excluded    |
| Global Settings                      | Preferences：主题、显示语言、Agent 输出语言                                                           | P0     | bundle `Hpe` 与 `globalSettings.preferences.*`                                                             | 主题与界面偏好本地/SQLite 持久化，设置页和深色即时渲染已验证；完整语言切换不完整                                                               | partial     |
| Global Settings                      | System、Network、Environments、Updates                                                                | P1     | `globalSettings.system/network/environments/updates.*`                                                     | 展示真实模型/runtime/资源状态；远程设备、环境管理与自更新不提供，保留明确能力边界                                                              | degraded    |
| Auth/Plan/Usage                      | 登录、relogin、profile、plan、usage、claim                                                            | P2     | frontend-auth/user APIs；auth expired modal                                                                | 本地单用户免登录；模型凭据仅在 API 进程，设置页不返回 key；云账号/plan/claim 不适用                                                            | degraded    |
| Extensions/Remote Employees/Missions | 插件 UI、远程设备、任务 debug/audit、QA extensions                                                    | P2     | `/extensions`、`/employees/*`、`/missions/*`、`/ext/:moduleId`                                             | 无本地等价且不属于成功主路径；仅在偏差文档保留记录                                                                                             | excluded    |
| Feedback/Updates                     | 可拖动反馈 FAB、升级 banner、restart card                                                             | P2     | `feedback.*`、`version-upgrade-banner.webp`、`update-restart-card-art.png`                                 | 未实现应用内自更新或旧版反馈 FAB；本地日志替代不作为该页面能力                                                                                 | excluded    |

## 关键端到端流程

| 流程           | 步骤                                                                          | 当前验收结论                                                                                                                                  | 状态        |
| -------------- | ----------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 首次创建 Waker | `/management` 空态 → 创建 → 选模板/自定义 → 填 profile/runtime → 保存 → Home  | P0 Ego 在桌面与 390px 创建新 Waker，创建后保留管理上下文并显示 Chat/Knowledge/Project 三个真实下一步入口；旧版 runtime/Skills 步骤仍简化      | implemented |
| 对话与恢复     | 选择 Waker → 新任务 → 选项目/模型 → 输入/附件 → 发送 → 流式输出 → 完成 → 刷新 | Goal 2 验证附件；Goal 3 真实 Kimi 返回长回答、代码、计划与 4 个 live/5 个 persisted 过程，2 个结构化来源经 SQLite sidecar 与 rollout 刷新恢复 | verified    |
| 错误与中断     | 对话中 stop；模拟 auth/network/quota；触发 approval/question                  | stop 后出现“回复已中断/继续”，注入网络故障可 retry；auth/quota 与 approval/question 原 Thread 闭环未全验收                                    | partial     |
| 知识导入与检索 | 创建知识库 → 导入 Markdown/文件/URL → 等待索引 → keyword/vector/hybrid 搜索   | P0 Ego 导入 Markdown，坏类型进入 partial-success/Needs check，重建后唯一 token 混合检索引用 `p0-knowledge-import.md#L1-L6`；URL 抓取明确排除  | verified    |
| Waker 使用知识 | Waker Knowledge → bind → 对话提问 → 查看引用 → unbind → 再检索                | 已完成 binding 生命周期；Goal 3 仅把 API 结构化来源中的 `[n]` 转为可点击定位，展示 notebook/document/chunk/mode/score/excerpt 并刷新恢复      | verified    |
| Memory 版本    | 对话产生 memory → 查看 timeline → 编辑 → 制造版本冲突 → rollback              | 编辑、快照、diff、dry-run/apply rollback 与 timeline 已验证，冲突有测试；对话自动产出 memory 未验证                                           | implemented |
| 项目与会话     | 创建本地目录/Git 项目 → Waker 绑定 → 新会话选择项目                           | 创建 READY 项目、选择项目后的真实 Codex 回复及 session_contexts/workingDirectory 回读已验证                                                   | verified    |
| Automation     | 创建 interval/cron → Run now → 查看运行与关联会话 → pause                     | Run now/history/pause/resume 与任务刷新已验证，due scheduler 有测试；运行关联会话和全部调度约束未实现                                         | implemented |
| Workflow       | 创建 → diagram/script 编辑 → trial run → ask-user → resume → 查看 trace       | 脚本流程运行状态、waiting_input/resume/succeeded、事件 trace 和 cancel 已验证；diagram 节点编辑尚未实现                                       | partial     |
| 窄屏与主题     | 桌面/窄屏导航与主要表单；切 light/dark；启用 reduced motion                   | 桌面、390×844 Knowledge/Waker、深色模式均无溢出且可操作；reduced-motion 代码存在但未单独留浏览器快照                                          | implemented |

## 完成门禁

- 当前自动化门禁已全绿；其证明范围包括各 SQLite store、Codex runtime、API 合同、SSE/恢复状态与 Web 组件测试。具体浏览器结论仍以 `docs/validation/ego-lite-2026-08-28.md` 为准。
- 两轮 Ego Lite task space 均已关闭；最终未解释的 runtime exception、console error、非预期 failed request 或 HTTP ≥400 为 `0`，桌面、移动和深色视觉证据已保存。
- 本地成功主路径已经覆盖真实 Codex 对话/恢复、附件与结果、项目 context、三种知识检索、Memory 版本、Automation 运行管理、Workflow trace、Connectors、Permissions 和 Human Actions。
- 本矩阵仍有 `partial` 项，尤其是旧版群组/设备语义、Mermaid/语法高亮、远程 URL 抓取、Automation 全部调度约束、Workflow diagram、Board lanes 与语言切换。因此当前证据支持“本地核心工作台可交付并可验证”，不支持“旧版 0.4.2 每项能力逐一等价复刻”。
- `degraded`/`excluded` 项不阻塞本地成功主路径，但必须继续保持可见说明，不得用 seed 或演示数据伪装为真实企业连接。

## 统一状态矩阵

| 区域                | Empty                                   | Loading                           | Success/Active                           | Error/Partial                                                   | Disabled/Read-only                                  |
| ------------------- | --------------------------------------- | --------------------------------- | ---------------------------------------- | --------------------------------------------------------------- | --------------------------------------------------- |
| Waker 管理          | no Wakers、no groups、no filter matches | workers/groups/remotes            | online/busy/recent activity              | worker/group/remote partial load failed                         | runtime offline、remote config unsupported          |
| Chat                | no Waker、no sessions、no messages      | startup、older messages、models   | streaming、completed、tool/plan progress | interrupted、network/auth/quota/rate-limit、attachment failures | waiting approval/question、session blocked          |
| Projects            | no projects/no memories                 | initializing                      | ready/public/private                     | create/delete/retry failed                                      | public profile read-only、remote source unavailable |
| Knowledge           | no notebooks/sources/results            | loading/uploading/indexing        | ready/connected                          | duplicate/quota/upload/index/bind partial success               | featured read-only、auth required                   |
| Memory              | empty scope/history/version             | document/timeline/version loading | saved/snapshot/rollback                  | save conflict、timeline/revert/rollback failed                  | historical snapshot read-only                       |
| Automation          | no triggers/no runs/no events           | source probe/saving/running       | enabled/active/completed                 | validation/source/webhook/run failed                            | paused、max-runs/end-date reached                   |
| Workflow            | no workflows/no runs/no versions        | customize/compile/run/trace       | queued/running/succeeded                 | blocked/failed/partial correction                               | paused/cancelled/official stale                     |
| Board/Human Actions | empty lanes/no approvals                | tasks/actions loading             | pending/running/waiting/done             | failed lane/partial error                                       | handled/ignored                                     |
| IM                  | no channels/conversations/pairings      | plugin/setup/pairing              | connected/active                         | credentials/network/TLS/session errors                          | stopped/paused/provider unavailable                 |
| Settings            | no devices                              | profile/status/update check       | saved/up-to-date/online                  | save/diagnostic/update failed                                   | unsupported capability/current device cannot unbind |

## 可复用资产

| 类别        | 文件                                                                                                                                         | 用途                                           |
| ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| 品牌        | `qoderwake-icon-light.svg`、`qoderwake-icon-dark.svg`、`qoderwake-icon-cn.svg`、`qoderwake-icon-global.svg`                                  | App Rail 品牌与主题切换                        |
| 字体        | `fonts/dm-sans/dm-sans-latin-normal.woff2`、`dm-sans-latin-italic.woff2`                                                                     | 旧版主要拉丁字体                               |
| 新任务参考  | `new-task-page.png`（2880×1800）                                                                                                             | 左侧栏、居中 task composer、项目/模型/附件布局 |
| Waker 引导  | `home-deco.png`、`qmate-quick-config-bg.png`、`qmate-quick-config-bg-dark.png`、`qmate-setup-guide-bg.svg`                                   | Home 和 quickstart                             |
| 空态/反馈   | `empty-project-icon.svg`、`success-check.svg`、`spinner.svg`、`hourglass.gif`、`hourglass-dark.gif`、`qoder-loading.png`、`pixel-smiley.png` | 异步状态、成功和空态                           |
| Team/IM     | `team-group-new-task-deco.png`、dark 版、`leader-symbol.gif`、dark 版、`im-assets/at-waker/*`                                                | 群聊、leader、@Waker 向导                      |
| IM Provider | `im-icons/dingtalk.svg`、`feishu.svg`、`weixin.svg`、`wecom-bot.svg`、`qq.svg`、`aliding.svg`                                                | Channel/provider 卡片                          |
| 功能卡      | `assets/create-card-bg-light-CybSbzeC.svg`、dark 版、`assets/experts-CAjLaGwQ.png`、`group-chat-DgnfK3nt.png`、`knowledge-BrmWyLEE.png`      | 创建入口和功能引导                             |
| 更新        | `version-upgrade-banner.webp`、`update-restart-card-art.png`                                                                                 | 仅用于保留旧版视觉语义；本地自更新降级         |

资产可以复用视觉语义，但不得把 Qoder 商标、账号、计划或云端能力当作本地功能已经实现的证据。

## Circle 结论

对恢复源码检索后，未发现产品级 `Circle` 类、数据模型、接口或前端抽象。命中的 `circle` 均为 SVG `<circle>`、圆形头像、上传组件 `picture-circle`、CSS radial-gradient 等表现层词汇。因此本项目将“本地 Circle 类”解释为本地 SQLite 数据层是合理的。

本地落地命名应以 `database`、`repository`、`knowledge store` 或项目最终确定的 SQLite 抽象为准，不应为了兼容一个没有旧版证据的名字额外创建 `Circle`。如果后续获得新的原始源码或产品文档证明 Circle 是业务概念，再补 ADR 和适配层。

## 明确偏差原则

1. QMind 云知识库改为本地 SQLite + FTS5 + 可替换向量适配器，保留 notebook/source/binding/引用的用户语义。
2. Qoder 模型与会话运行时改为服务端 `@openai/codex-sdk` Thread API，Web 不直接接触 SDK、CLI 或凭据。
3. 企业 IM、远程员工、Aone/DWS、云分享、多用户 Public Projects、计划/Usage 与自更新可以降级，但页面必须说明原因、影响和可用替代。
4. 任何 `degraded` 项不能用假数据表现为真实外部连接成功；演示模式必须带明确标识。
5. P0 全部和 P1 的本地可实现部分完成自动化与 Ego Lite 验证前，不能宣称旧版主要前端已复刻完成。
